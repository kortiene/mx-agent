# Formatting expectations for the mx-agent workspace.
# Only stable rustfmt options are used so `cargo fmt --check` is deterministic
# across toolchains.
edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
